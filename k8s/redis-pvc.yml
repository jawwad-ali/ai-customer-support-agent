apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc
  namespace: crm
  labels:
    app: redis
    component: cache
    part-of: crm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
