apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: crm
  labels:
    app: api
    component: backend
    part-of: crm
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
