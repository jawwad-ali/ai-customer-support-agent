apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: crm
  labels:
    app: web
    component: frontend
    part-of: crm
spec:
  type: NodePort
  selector:
    app: web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30000
      protocol: TCP
