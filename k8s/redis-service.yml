apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: crm
  labels:
    app: redis
    component: cache
    part-of: crm
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
